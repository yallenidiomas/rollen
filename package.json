{
  "name": "rollen",
  "version": "1.1.0",
  "description": "Grimório e Dados 3D para D&D 2024",
  "scripts": {
    "build": "rimraf dist && mkdir dist && copyfiles -u 0 *.png *.ico *.svg *.webmanifest master.png *.mp3 dist && javascript-obfuscator script.js --output dist/script-real.js && javascript-obfuscator regras.js --output dist/regras-real.js && html-minifier-terser --collapse-whitespace --remove-comments index.html -o dist/index.html && cleancss -o dist/style.css style.css",    
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "watch": "nodemon --watch script.js --watch regras.js --watch style.css --exec \"npm run build\"",
    "up": "npm run build && git add . && git commit -m \"Update Auto\" && git push && npm run deploy"
  },
  "keywords": [
    "rpg",
    "dnd",
    "dice",
    "3d"
  ],
  "author": "Yallen Carvalho",
  "license": "All Rights Reserved",
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "copyfiles": "^2.4.1",
    "gh-pages": "^6.3.0",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^5.3.0",
    "nodemon": "^3.1.11",
    "rimraf": "^6.1.3"
  }
}
